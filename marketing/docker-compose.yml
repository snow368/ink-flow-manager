version: "3.9"
services:
  inkflow-server:
    build: .
    ports:
      - "8787:8787"
    env_file:
      - .env
    environment:
      - PORT=8787
    volumes:
      - ./server/data:/app/server/data
    restart: unless-stopped
