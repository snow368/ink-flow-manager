---
# Redirect from old /features/pos to /features/payments
import type { APIRoute } from 'astro';
export const GET: APIRoute = ({ redirect }) => {
  return redirect('/features/payments', 301);
};
---
