{
  "name": "inkflow-marketing",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:prod": "astro build",
    "preview": "astro preview",
    "submit-indexnow": "node scripts/submit-indexnow.mjs",
    "i18n:extract": "node scripts/extract-strings.mjs",
    "i18n:build": "astro build && node scripts/build-es.mjs",
    "deploy": "npm run i18n:build && wrangler pages deploy dist --project-name ink-flow && npm run submit-indexnow"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.0.0",
    "@astrojs/tailwind": "^6.0.0",
    "@tailwindcss/typography": "^0.5.20",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "wrangler": "^4.110.0"
  }
}
